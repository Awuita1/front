import React from 'react';

const Header = (props) => {
    return(
        <header>
            <div>
                <img src= "img/logo.jpg" width={"100"} alt='Kimow' />
            </div>
        </header>
    );
}

export default Header;